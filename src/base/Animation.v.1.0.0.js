export default class {
    constructor(t, e = {}) { this.attributeToAnimateName = t.attributeToAnimateName, this.fromSecond = t.fromSecond, this.toSecond = t.toSecond, this.readOnlyElementAttrNames = t.readOnlyElementAttrNames, this.argsForAlgo = e, this.fps = 60, this.state = {}; }
    animate(t, e, a = {}) { return !0; }
}
